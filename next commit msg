features or elts added/modified since last commit (current branch = 'master') :
- text is now justified on 'seances' page
- margin-bottom on 'seances' page's cards
- text is more "aéré", and some elements are bolds, on this same page
- idem on "detail" page